# Перевірка inference-образу, 07.09.2026

Початковий GitHub CI report: Python - 4 HIGH; Debian 13.6 - 51 HIGH та 3 CRITICAL. Кількість означає знахідки пакет/CVE, а не обов'язково унікальні CVE.

Виконані зміни:
- FastAPI 0.141.1 та Starlette 1.6.0; ML-залежності й модель не змінено.
- Base image `python:3.13-slim-trixie`; під час збірки виконано `apt-get update` / `upgrade`. На момент перевірки оновлень не було.
- Після встановлення залежностей виконано `pip check` та видалено непотрібні runtime pip/setuptools/ensurepip. Це прибрало також знахідки для інструментів встановлення без додавання зайвих runtime-залежностей.
- API працює від UID/GID 10001, а не root. Це обмеження привілеїв, а не виправлення OS CVE.

Повторно зібрано й проскановано локальний образ `localhost/final-inference:security-check`, image ID `sha256:7b9fa5d11ace6b7471ff061f226943c7b0662745d8257afef7d9c8afe8aac971`, Trivy 0.74.0:

| Компонент | HIGH | CRITICAL |
| --- | ---: | ---: |
| Python | 0 | 0 |
| Debian 13.6 | 51 | 3 |

Для залишкових HIGH/CRITICAL поле FixedVersion порожнє. Це не доказ відсутності ризику або неможливості експлуатації. Findings не приховано через ignore-файл; CI gate залишається блокувальним. Наступні дії: перевіряти security updates Debian або окремо тестувати сумісний base image з меншим набором OS-пакетів, після чого повторити scan.

26 локальних тестів пройшли. У контейнері перевірено `/health`, `/predict`, `/metrics`; модель повернула очікуваний клас 0. Тестовий контейнер видалено, Podman VM зупинено. ECR/Production не змінювалися. Для підтвердження в GitHub потрібен push і новий запуск `image-security`; наведений повторний scan є локальним.
